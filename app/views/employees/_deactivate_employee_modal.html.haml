#deactivate_employee_modal.modal.fade{ 'aria-labelledby' => 'myModalLabel', role: :dialog, tabindex: '-1' }
  .modal-dialog{ role: 'document' }
    .modal-content
      .modal-header
        %button.close{ 'aria-label' => 'Close', 'data-dismiss' => 'modal', type: :button }
          %span{ 'aria-hidden' => 'true' } ×
        %h4.modal-title Modal title
      .modal-body
        = form_for(@employee, url: deactivate_employee_path) do |f|
          .form-group
            = f.label :end_date, 'Date of Leaving'
            = f.text_field :end_date, class: 'form-control employee-termination-date', id: 'datepicker'

          .form-group
            %button.btn.btn-default{ 'data-dismiss' => 'modal', type: :button } Close
            = f.submit 'Save changes', class: 'btn btn-primary pull-right'
